
export default function HomePage() {
  return (
    <div style={{ padding: '20px', border: '2px dashed green', margin: '20px', backgroundColor: 'lightgreen' }}>
      <h2>PAGE.TSX - MINIMAL CONTENT</h2>
      <p>If you see this, the page component is rendering within the diagnostic layout.</p>
      <p>Check the terminal for error messages if issues persist.</p>
    </div>
  );
}
